# Freshdesk-frontend
This project is the frontend portal for integrating Freshdesk and HubSpot.

# First time run the project
install all required libraries: npm install

# Run Project
Start the project in development mode: npm run dev

# Build Project
Before creating the build, make sure the backend API base URL is set to the production URL.
Create a production build before deployment: npm run build

